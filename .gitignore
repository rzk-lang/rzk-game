# Build outputs
dist-newstyle/
public/
node_modules/
*.wasm
ghc_wasm_jsffi.js

# Local cabal overrides
cabal.project.local
cabal.project.local~

# Haskell
*.hi
*.o

# Nix
result
result-*
.direnv/

# Local planning notes, drafts, and scratch docs (roadmap, handoffs, PR drafts).
# A plain directory or a symlink into a personal-notes checkout both match.
notes
notes/

# Phase 0 de-risking spike (kept locally, not part of the engine)
spikes/
